services:
  backend:
    image: ${DESKTOP_PLUGIN_IMAGE}
    command: /backend/mcp-gateway-manager
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - mcp-config:/config
    ports:
      - "9102:9102"

volumes:
  mcp-config:
